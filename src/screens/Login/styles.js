import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        padding: 30
    },
    input: {
        marginBottom: 20
    },
    buttonText: {
        flex: 1,
        textAlign: 'center'
    }
});